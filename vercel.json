{
  "version": 2,
  "buildCommand": "bun install && bun run build && cp dist/index.html dist/client/",
  "outputDirectory": "dist/client",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
